/** Demo netlists shipped under editors/vscode/demo/ and copied by Create Demo Folder. */

export const DEMO_FOLDER_NAME = "spice-lsp-demo";

/** Relative paths under the extension's `demo/` directory (and the created workspace folder). */
export const DEMO_FILE_NAMES: readonly string[] = [
  "README.md",
  "same-file.sp",
  "models.sp",
  "top.sp",
  "corners.lib",
  "top-lib.sp",
];
